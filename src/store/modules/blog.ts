// TODO: get json with blog posts as initial state from external file

// initial state
const state = {
  posts: [],
};

// getters
const getters = {};

export default {
  //namespaced: true,
  state,
  getters,
};
